{
  "name": "root",
  "private": true,
  "workspaces": [
    "@kth/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "lerna run dev --stream --parallel --scope=@kth/style"
  },
  "devDependencies": {
    "lerna": "^6.4.1"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.6",
    "react-popper": "^2.3.0"
  }
}
