{
  "name": "root",
  "private": true,
  "workspaces": [
    "@kth/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "lerna run dev --stream --parallel --scope=style-web",
    "build": "lerna run build",
    "publish": "lerna publish --no-private"
  },
  "devDependencies": {
    "lerna": "^7.3.1",
    "prettier": "^3.0.3",
    "prettier-plugin-astro": "^0.12.0"
  }
}
