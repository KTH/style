{
  "name": "root",
  "private": true,
  "workspaces": [
    "@kth/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "lerna run dev --stream --parallel"
  },
  "devDependencies": {
    "lerna": "^6.4.1"
  }
}
